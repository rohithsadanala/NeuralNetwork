# Neural Network from Scratch (MNIST)

This project implements a fully connected Neural Network using only **Python** and **NumPy**. It classifies handwritten digits (0-9) from the MNIST dataset without using deep learning frameworks like TensorFlow or PyTorch.

## 🚀 Features
- **No Black Boxes:** Built purely with matrix mathematics.
- **Architecture:** Input Layer (784) → Hidden Layer (128, ReLU) → Output Layer (10, Softmax).
- **Optimization:** Mini-batch Gradient Descent.
- **Accuracy:** Expect ~95%+ accuracy on the test set after 10 epochs.

## 🛠️ Requirements
You need Python 3.x and the following libraries:
```bash
pip install numpy scikit-learn
